//
// Created by rotem on 15/01/17.
//
#include <stdlib.h>

#include "../include/BidiMessegeEncoderDecoder.h"

/**
* This code assumes that the server replies the exact text the client sent it (as opposed to the practical session example)
*/
/*
int main () {

    BidiMessegeEncoderDecoder* encdec = new BidiMessegeEncoderDecoder();

        const short bufsize = 1024;
        char buf[bufsize];
        std::cout << "waiting for user input\n" << std::endl;
        std::cin.getline(buf, bufsize); //userinput

        //now need to convert the user input to the format for the server (using encoderDevoder).
        for (int i=0;i<4;i++){
            std::cout << " " <<readyToSend[i]<< std::endl;
        }
        std::string line=(readyToSend);
        int len=line.length();
        std::cout <<std::endl;
        std::cout << "length of string to send:\n" << len <<std::endl;
        std::cout << "string after encode:\n" << line <<std::endl;




        return (0);
}
 */


